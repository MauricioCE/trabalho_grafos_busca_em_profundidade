export type Vector2 = {
  x: number;
  y: number;
};

export type Size = {
  width: number;
  height: number;
};

export type Range = {
  min: number;
  max: number;
};

export type Direction = "up" | "down" | "left" | "right";

export type MovementKeys = {
  up: string;
  down: string;
  left: string;
  right: string;
};
